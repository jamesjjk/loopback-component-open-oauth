###*
# Module dependencies.
###

{ ServerError } = require '../errors/server-error'

###*
# Constructor.
###

class TokenResponseType
  constructor: ->
    throw new ServerError 'NOTIMPL'
    return

module.exports = TokenResponseType